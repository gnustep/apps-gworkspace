{
    fsn_info_type = <*I0>;
    geometry = "1000 646 450 300 0 0  1536 1200 ";
    iconposition = <*I5>;
    iconsize = <*I48>;
    labeltxtsize = <*I12>;
    lastselection = (
    );
    singlenode = <*BY>;
    spatial = <*BY>;
    viewtype = Icon;
}